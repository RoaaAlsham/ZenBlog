{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "JwtTokenOptions": {
    "//": "Real values must NOT live here for anything beyond local dev. In staging/production, override SecretKey/Issuer/Audience via environment variables (e.g. JwtTokenOptions__SecretKey) or a secrets manager. Without a real SecretKey the app will throw at startup/login instead of silently issuing an insecure token.",
    "Issuer": "",
    "Audience": "",
    "SecretKey": "",
    "ExpirationMinutes": 60
  }
}
