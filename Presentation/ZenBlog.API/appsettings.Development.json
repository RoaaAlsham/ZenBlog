{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "JwtTokenOptions": {
    "//": "Local development only. HS256 needs a key of at least 256 bits (32 chars); this one is intentionally long enough to satisfy that. Never reuse this value outside a developer's own machine - it is committed to source control precisely so it is NOT considered secret.",
    "Issuer": "ZenBlog.API.Dev",
    "Audience": "ZenBlog.Client.Dev",
    "SecretKey": "dev-only-signing-key-please-replace-me-32bytes-min",
    "ExpirationMinutes": 60
  }
}
